# GDI Analysis Notebooks

## Running the Analysis Notebook

```bash
cd data/notebooks
poetry install
poetry run jupyter notebook gdi_scoring_analysis.ipynb
```

The notebook will open in your browser automatically.

## What's Here

- `gdi_scoring_analysis.ipynb` - Network concentration analysis
- `pyproject.toml` / `poetry.lock` - Dependencies (self-contained)
